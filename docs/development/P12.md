# P12 — MCP Integration

**Status:** LOCKED  
**Type:** integration  
**Purpose:** Add deny-by-default MCP capability discovery through Tool Runtime.

## 1. Objective

Deliver only the bounded MCP Integration scope while preserving every accepted P0–P9 architecture, security, privacy, testing, and cross-platform boundary.

## 2. Required Reading

- `AI_COLLABORATION_CHARTER.md`
- `AGENTS.md`
- `.laes/CURRENT_PHASE.yaml`
- `docs/standards/Engineering_Standard.md`
- `docs/architecture/System_Architecture.md`
- `docs/standards/Testing_Standard.md`
- `docs/standards/Security_Standard.md`
- `docs/standards/Cross_Platform_Standard.md`
- `docs/development/P12.md`
- accepted ADRs/RFCs directly relevant to this phase

## 3. Allowed Work

- MCP transport adapters and lifecycle.
- Schema validation, allowlists, timeouts and cancellation.
- Permission and malicious-server tests.

## 4. Forbidden Work

- Unreviewed remote execution.
- Direct filesystem or credential access.
- Plugin runtime implementation.
- Work assigned to any later P stage.
- Advancing `.laes/CURRENT_PHASE.yaml` without the required gate.

## 5. Architecture Invariants

- MCP capabilities enter through Tool Registry and Runtime.
- Network access and server trust are explicit.
- Remote errors and schemas are treated as untrusted.
- Provider, Tool, Workspace, and Credential Runtime boundaries remain intact.
- Windows, Linux, and macOS remain first-class targets.

## 6. Deliverables

- Phase contracts and implementation limited to the allowed scope.
- Tests and security evidence for each acceptance criterion.
- Required ADR/RFC updates approved before architectural implementation.
- `P12_COMPLETION_REPORT.md` with scope, root cause, changes, results, risks, rollback, and deviations.

## 7. Acceptance Criteria

- No server is enabled by default.
- Unauthorized capabilities cannot execute.
- Timeout, disconnect and schema-spoof cases pass.
- P0–P11 regression validation remains green.
- No unrelated feature, framework replacement, or future-phase work is present.

## 8. Required Validation

```text
python -m compileall app tests
python -m ruff check app tests
python -m pytest tests -q
```

Also run offline protocol, permission, timeout and malicious-server tests. Run frontend, desktop, packaging, or platform matrices when affected by this phase.

## 9. Completion Report

Report root causes, modified files, concise diff, exact commands/results, security and privacy impact, cross-platform impact, migrations, rollback, remaining risks, and specification deviations.

## 10. Mandatory Stop Gate

**STOP after P12.** P13 remains forbidden until this phase receives ChatGPT PASS and explicit owner approval.

